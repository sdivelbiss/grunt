module.exports = function(grunt){
    // Config
    grunt.initConfig({
        //pass in options to plugins, references to files, etc.

    });

    // Load plugins
    //grunt.loadNpmTasks('')

    //Register tasks

}